# 작업 규칙

구현 사양은 ARCHITECTURE.md다. 충돌하면 ARCHITECTURE.md를 따른다.
DECISIONS.md는 판단 근거 기록이다. 구현 시 참조하지 않는다.

프론트엔드는 HTML, CSS, 바닐라 자바스크립트로만 작성한다. 라이브러리와 프레임워크를 도입하지 않는다.

백엔드에 새 패키지가 필요하면 requirements.txt에 추가하고 버전을 고정한다.

API 키는 서버리스 함수 안에서만 사용한다. 프론트엔드 코드에 키를 넣지 않는다.

색상은 CSS 변수로 정의한다. 색상값을 선택자 안에 직접 쓰지 않는다.

사용자 입력과 외부 API 응답은 textContent로 삽입한다. innerHTML을 쓰지 않는다.

AI가 반환한 번호로 서버가 보관한 원본을 꺼내 쓴다. AI가 반환한 장소명을 그대로 사용하지 않는다.

에러 메시지와 로그에 스택 트레이스, 내부 경로, 환경 변수 이름, 요청 URL을 넣지 않는다.

한 번에 한 파일씩 구현한다. 여러 파일을 동시에 만들지 않는다.

주석과 사용자 노출 문구는 한국어로 쓴다.

커밋하지 않는다. 변경 사항만 보고한다.
