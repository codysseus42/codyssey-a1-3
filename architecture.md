1. 시스템구성
- GitHUb 연동을 통해서 vercel 서버리스로 배포될 서비스를 개발한다. 프론트엔드는 웹환경을 백엔드는 python을 이용한다.
- 프론트 앤드는 반드시 html,css,vanila javascript만을 사용하며 일체의 라이브러리나 프레임 워크는 사용하지 않는다.
- 프로젝트 디렉토리 구조는 반드시 html이 있는 디렉토리(./service) 아래로 다음과 같이 구성된다.
	1. service 디렉토리
		index.html을 비롯한 html, python backend 를 위한 라이브러리 requirements.txt,이미 위치한 .env,.env.example 은 건드리지 않는다.
	2. service 하부 디렉토리
		바로 아래에 다음 디렉토리들에 해당 속성 파일들이 위치한다.
		 /css, /js, /api ,/images
- 기본 레이아웃과 스타일이 모든 화면에서 공통으로 적용되어야한다.
- 반드시 모바일,테블릿,데스크팁 환경 3개를 염두에 둔 반응형으로 개발되어야 한다. 레이아웃이 깨져서는 안된다.
- 반드시 vanila javascript,css,html만 사용한다.
- 응답이나 입력값 따라서 오류를 사용자에게 공지한다.
- 브라우저는 필요할 경우 chrome을 상정하되 중립적으로 개발한다.
- Vercel Serverless Functions - Python을  fetch('/api/..')로 호출하고 응답을 처리한다.
- 백엔드에서 필요한 패키지는 requirements.txt에 정의하고 추가된 경우 반드시 현행화 한다.
- .env.example을 참고하여 dotenv 패키지를 이용해서 api키를 읽어서 사용한다.
- api prompts에 있는 프롬프트를 활용하여 3.API계약에 맞추어 추천 여행지를 제공 받는다.
2. 데이터 흐름
- 입력화면(사용자 입력) 희망 여행지,희망사항 -> javascript 빈값 체크 -> llm 내용적절성 확인 오류가 없을 경우 진행  ->python 함수(선택 여행지를 확인하고 Tour api 호출) 문제가 없을 경우 진행(재시도 실패시 사용자에게 다시 물음 허가시 여행정보 없이 계속 진행) -> python함수 여행지 관련 정보를 활용하여 llm호출최종 기획서 안내 -> 결과화면에 표시
- 호출 오류는 (4xx/5xx), 지연/타임아웃 사용자에게 알려주고 재시도를 유도한다.
- 단 llm호출에 있어서 특정 모델이 바쁠경우(오류 이름과 메시지를 모르겠네) 대체 모델을 사용 할지 유저에게 묻고 동의할 경우 계속 진행한다.
- 여태까지 받은 입력값과 결과값을 쿠키에 저장하여 oo여행계획 (입력시간) 식 으로 나의 여행 계획 목록에서 볼수 있다. 클릭화면 입,출력 화면에 다시 값을 넣어서 표시해준다.


3. API 계약
- {

4. 프론트엔드
- 필수 값을 입력단계

5. 백엔드
	- 
6.실패 처리
-
7.환경 변수
	1.GEMINI_API_KEY=제미나이 llm활용을 위한 api키
     2.TEXT_MODEL=사용할 제미나이 모델 버전 본 배포환경에서는 gemini-3.6-flash 예정
     3.TEXT_MODEL_FALLBACK= 2.TEXT_MODEL실패할 경우 유저에게 묻고 사용할 보충 모델 본 배포환경에서는 gemini-3.5-falsh 예정 